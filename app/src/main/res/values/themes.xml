<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="App" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryLightColor</item>
        <item name="colorOnPrimary">@color/primaryTextColor</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="colorOnSecondary">@color/secondaryTextColor</item>

        <item name="background">@color/primaryColor</item>
        <item name="colorOnBackground">@color/primaryTextColor</item>
        <item name="colorSurface">@color/primaryLightColor</item>
        <item name="colorOnSurface">@color/primaryTextColor</item>

        <item name="colorAccent">@color/danger</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/primaryDarkColor</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="App.Fullscreen" parent="App">
        <item name="android:background">@color/primaryColor</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="App.NavigationView" parent="ThemeOverlay.Material3.NavigationView">
        <item name="colorPrimary">?attr/colorAccent</item>
    </style>

    <style name="App.Toolbar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">15sp</item>
    </style>
</resources>